export default function InterviewProgress({ questionNumber, topics }) { return <section className="side-section"><div className="section-heading"><span>Interview progress</span><b>{questionNumber} / 8</b></div><div className="progress-track large"><i style={{ width: `${(questionNumber / 8) * 100}%` }} /></div><div className="topic-list">{topics.map((topic, index) => <div className={index < questionNumber ? 'covered' : index === questionNumber ? 'current' : ''} key={topic}><span>{index < questionNumber ? '✓' : String(index + 1).padStart(2, '0')}</span>{topic}</div>)}</div></section>; }
